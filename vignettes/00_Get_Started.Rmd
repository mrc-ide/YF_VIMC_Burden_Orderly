---
title: "00 - Get Started"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{00 - Get Started}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

The code in this repository can be used to run yellow fever burden calculations for one or more countries and for one or more scenarios using the dynamic yellow fever transmission model in the [YEP](https://github.com/mrc-ide/YEP/) package, using standard VIMC touchstone inputs. It uses the [orderly](https://github.com/mrc-ide/orderly/) package to organize calculations and aid reproducibility.

To start using the code in the repository, ensure the correct version of the YEP package is installed and loaded :

```{r,eval=FALSE}
devtools::install_github("mrc-ide/YEP",ref="Version-0.2")
library(YEP)
```

Install orderly:

```{r,eval=FALSE}
install.packages("orderly",repos = c("https://mrc-ide.r-universe.dev", "https://cloud.r-project.org"))
library(orderly)
```

Initialize the repository folder for use with orderly. The working directory should be set to the repository folder.

```{r,eval=FALSE}
path=getwd()
orderly::orderly_init(path) 
```